[init]
	defaultBranch = main
[color]
	ui = auto
[pull]
	rebase = false
[core]
	editor = code --wait
[includeIf "gitdir:~/**"]
    path = ~/.gitconfig_corpo
[includeIf "gitdir:~/ReposC/**"]
    path = ~/.gitconfig_corpo
[includeIf "gitdir:~/ReposP/**"]
    path = ~/.gitconfig_perso
