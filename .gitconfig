[init]
	defaultBranch = main
[color]
	ui = auto
[pull]
	rebase = false
[core]
	editor = code --wait
[includeIf "gitdir:~/**"]
    path = ~/.gitconfig_corpo
[includeIf "gitdir:~/Corporate/**"]
    path = ~/.gitconfig_corpo
[includeIf "gitdir:~/Personal/**"]
    path = ~/.gitconfig_perso
